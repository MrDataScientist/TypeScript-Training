//const player:anykugli = {firstName:"magic",lastName:"johnson",address:"staples"};

const _= require('lodash');

const colors = ["red","blue","green"];

const firstColor= _.first(colors);
console.log(firstColor);